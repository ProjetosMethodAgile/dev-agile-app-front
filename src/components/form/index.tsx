import FormComponent from "./FormRoot";
import FormButton from "./FormButton";
import FormError from "./FormError";
import FormInput from "./FormInput";

export const Form = {
  Root: FormComponent,
  Button: FormButton,
  Error: FormError,
  Input: FormInput,
};
